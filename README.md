# CRIANDO O .venv:

- `python3 -m venv .venv # cria o virtual environment`

# USANDO O .venv

- `source .venv/bin/activate`

# INSTALANDO AS DEPENDENCIAS NECESSARIAS

- `pip install jupyter numpy matplotlib`

# RENDERIZANDO O PDF (Necessario instalar o Quarto antes)

- `quarto render`
